[package]
name = "planner-domain"
version = "0.1.0"
edition = "2021"
rust-version = "1.90"

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["std", "clock", "serde"] }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
